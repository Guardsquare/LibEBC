language: cpp

matrix:
  include:
    - os: osx
      osx_image: xcode7.3
      env: LLVM_HINT="/usr/local/opt/llvm"
    - os: linux
      sudo: required
      dist: trusty
      env: LLVM_HINT="/usr/share/llvm-3.8"

before_install:
  - ./.travis/dependencies.sh

script:
  - mkdir build
  - cd build
  - cmake .. -DCMAKE_BUILD_TYPE=Debug -DLLVM_HINT=$LLVM_HINT
  - make
  - cd test
  - ctest
